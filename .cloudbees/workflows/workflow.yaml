apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Publish action

on:
  push:
    branches:
      - "**"
  workflow_dispatch:
jobs:
  build:
    steps:
      - name: Checkout
        uses: cloudbees-io/checkout@v1

      - name: Run flow
        id: kestra
        uses: swashbuck1r/kestra-action@main
        with:
          namespace: acme
          flow: myflow
      - name: print output
        run: echo "myvar1: ${{ steps.kestra.outputs.myvar1 }}"